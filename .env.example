# Server configuration
PORT=8080

# Set to "production" to disable dev logging middleware
NODE_ENV=development

# Optional — override the default User-Agent sent when fetching pages. Leave
# unset to use the built-in default, which honestly identifies this tool.
# Maps to the `userAgent` field in configSchema (src/index.ts).
# USER_AGENT=
